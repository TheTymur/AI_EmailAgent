# AI_EmailAgent
It is a CLI Gmail assistant powered by Python and the Gemini API. Acting as a concise, sarcastic AI, it automatically sorts your inbox, filters spam, and summarizes threads. It builds context-aware drafts but strictly requires explicit user approval before sending any emails.
